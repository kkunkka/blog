# MySQL分析执行效率


` explain xxx`


### type

#### System

系统表并且只有一行内容

#### Const

等值查询，用到了主键或者唯一索引
返回值只有一行

#### eq_ref

连接查询，并且用到了主键或者唯一索引

#### ref

等值查询，用到了非唯一索引,

#### range

用到了索引，并且是范围查询，比如in(1,2,3) 或者是 between

#### index

用到了索引，扫描了整个索引树

#### All

完全没用到索引，走全表扫描